# 一期内部试运行最终收口报告

更新时间：2026-05-13

## 结论

一期内部 BIM/CAD 资产管理试运行能力已达到当前验收口径，测试 agent 极短复验确认当前无 P0/P1/P2。

当前可以进入公司内部用户试运行。后续一期范围内只建议处理试运行中发现的 P0/P1，不再主动扩展新能力。

## 已通过的核心范围

1. 真实 NAS 项目资产接管：
   - 已接管真实 NAS 项目 `16` 个。
   - 已登记正式文件资产 `40,935` 份。
   - 模型文件 `2,604` 份。
   - 图纸文件 `38,331` 份。
   - 总容量约 `296.02 GiB`。

2. 资产管理页面：
   - 首页项目下拉当前为 `18` 个项目。
   - 资产总览默认展示真实 NAS 项目。
   - 项目资产详情支持分页、筛选、详情、复制路径、人工治理、checksum 任务入口和预览状态入口。
   - 扫描任务页支持进度、状态、报告、取消、续扫。
   - 数据质量页可解释缺 checksum、非标准资料、失败扫描等风险。
   - 非标准资料治理页保留 `95`、`98`、`99`、投标资料、参考资料、未知目录等暂缓数据，不污染正式资产库。

3. 文件预览安全外壳：
   - PDF/图片、Office、CAD、BIM 等格式可展示预览状态和后续转换要求。
   - 当前不读取文件正文。
   - 当前不做真实 Office/CAD 转换。
   - 当前不做 BIM 轻量化。
   - 预览接口不返回真实 `storagePath`。

4. 权限与路径安全：
   - 普通项目用户无法访问未授权真实 NAS 项目。
   - 普通项目用户无法通过文件 ID 猜测读取无权限文件详情或预览状态。
   - 首页不再显示 `SCANCTRL-*` 测试项目。
   - 扫描控制脚本复跑后会自动清理自身测试项目，不再污染项目下拉。

5. 企业 Agent DB-2 准备状态：
   - 本机 dev 只读账号 `hermes_agent_ro` 已恢复可用。
   - 密码保存在 macOS 钥匙串，不写入仓库文档。
   - 只读账号只能读取四个稳定 View：
     - `ProjectAssetView`
     - `FileAssetView`
     - `ModelAssetView`
     - `AuditEventView`
   - 业务底表仍不可读。

## 最终复验依据

测试 agent 最新报告：

```text
handoff/test-agent/latest-report.md
```

复验确认：

```text
check-agent-db2-contract.sh 通过
首页项目下拉：18 个项目，无 SCANCTRL-*
check-scan-task-control.sh 复跑后不再留下可见 SCANCTRL-* 测试项目
当前无 P0 / P1 / P2
```

主 agent 修复报告：

```text
handoff/main-agent/phase1-short-regression-remediation-report.md
```

## 非阻塞项

1. 真实 NAS 仍有大量文件缺 checksum。
   - 已有 checksum 任务能力和试点验证。
   - 全量 checksum 不作为一期上线阻塞项。

2. 非标准目录仍有 `11` 条待治理。
   - 这是设计内状态。
   - 不进入正式资产库。
   - 后续可在企业 agent 接入后做辅助治理。

3. 前端构建仍可能出现 Vite chunk 体积警告。
   - 不阻塞一期内部试运行。

4. 企业 agent 尚未正式合并。
   - 平台侧 DB-2 只读边界已准备好。
   - 当前不做 Hermes 深度联调。

## 后续执行口径

允许：

1. 用户内部试运行。
2. 修复试运行发现的 P0/P1。
3. 补充用户操作说明、验收说明、交接说明。
4. 根据真实项目负责人反馈做窄范围可用性修复。

暂缓：

1. 真实 BIM 轻量化在线预览。
2. Office/PDF 正文抽取。
3. 向量库或全文索引。
4. Agent 自动治理、自动审批、自动删除。
5. 自动移动、删除或修复 NAS 文件。
6. 10TB 全量 checksum。

## 主 Agent 裁决

一期内部试运行能力可以正式认定为收口。

下一阶段建议：

1. 先组织内部用户按 `phase1-internal-trial-acceptance-checklist.md` 试运行。
2. 主 agent 只处理试运行中出现的 P0/P1。
3. 若用户确认进入二期，再单独规划二期下一批次，不与一期收尾混在一起。
