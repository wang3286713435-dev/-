
# 8C-GD-F4 极短验收记录：构件拾取、模型爆炸与属性代理

时间：2026-06-05

## 1. 结论

`8C-GD-F4：葛兰岱尔 Viewer 构件交互兼容修复` 极短验收通过。

当前可以确认：

- Viewer 前端已包含多坐标构件拾取逻辑。
- Viewer 已包含 DOM 点击兜底逻辑。
- 平台已补齐葛兰岱尔 worker 静态资源。
- 模型爆炸参数已对齐 demo，包含 `showAxis: true`。
- 后端已暴露受控构件属性代理接口。
- READY viewer ticket 明确声明构件拾取、模型爆炸、构件属性能力。
- 构件属性代理响应未泄露底层路径或密钥。

## 2. 验证命令

```text
curl -fsS http://127.0.0.1:8080/actuator/health
bash scripts/dev/check-8c-gd-f4-component-pick-blow-properties.sh
```

## 3. 验证结果

```text
后端健康检查：UP
8C-GD-F4 专项：PASS=22 FAIL=0
```

通过项包括：

- 前端包含多坐标拾取兼容逻辑。
- 前端包含引擎拾取失败后的 DOM 兜底等待逻辑。
- 前端包含多 tag 爆炸参数兼容逻辑。
- 前端模型爆炸参数包含 demo 使用的 `showAxis`。
- 前端拾取坐标优先使用 canvas 真实矩形。
- 前端为嵌入式 Viewer 提供平台本地引擎资源目录。
- 前端安装葛兰岱尔 worker 地址桥接。
- 前端会将缺失 worker 重写到平台本地资源。
- 前端兼容 `id / externalId / featureId` 等拾取返回字段。
- 前端包含构件属性展示区域。
- 前端接入平台代理构件属性接口。
- 平台本地静态资源包含构件拾取 worker。
- 后端暴露受控构件属性接口。
- 后端调用葛兰岱尔构件属性 API。
- 新提交转换任务启用属性库生成。
- 管理员登录成功。
- 可切换到 105 / `projectId=503`。
- 模型清单返回 OK 且未泄露底层路径。
- READY Viewer ticket 明确声明拾取、爆炸、构件属性能力。
- 构件属性代理响应 OK 且未泄露底层路径。

## 4. 当前能力边界

当前已完成：

- 模型打开。
- 构件拾取。
- 构件高亮。
- 构件属性平台代理。
- 模型爆炸。
- worker / wasm 静态资源本地兜底。

当前不能扩大承诺为：

- 完整 BIM 构件级搜索。
- 完整图模联动。
- 碰撞检查。
- 构件级交付审查。
- C塔交付中心已完成接入。

这些能力应进入后续 `8D / 8E` 或 C塔专项复用批次。

## 5. 主 agent 裁决

```text
8C-GD-F4-SHORT-VERIFY: PASS
```

本批可以作为当前轻量化 Viewer 基线，但建议后续测试 agent 如需正式收口，可再补一次浏览器实体点击确认。