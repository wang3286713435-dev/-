# 真实 NAS 试点验收报告

日期：2026-05-09  
主 agent：Codex  
验收范围：`100-深圳市二十八高项目`、`105-启航华居项目`

## 1. 验收边界

- 本轮仅做验收检查，不修改、不移动、不删除 NAS 原文件。
- 企业 agent 协议尚未确定，本轮不做 agent 接口联调。
- 本轮重点验证真实试点项目是否已达到“一期内部资产管理试点”标准：可建档、可检索、可追溯、可权限隔离、可审计。

## 2. 后端与数据库状态

- 后端启动成功。
- 健康检查：`UP`
- Flyway 当前版本：`13`
- 当前数据库体量约：`8.64 MB`
- 基础设施容器：MySQL、Redis、MinIO 均运行中。

## 3. 项目资产验收

### 100-深圳市二十八高项目

- 项目 ID：`504`
- 扫描任务：`415 / REAL_NAS_100`
- 扫描状态：`SUCCEEDED`
- 扫描文件数：`4634`
- 自动入库：`3668`
- 待审核产生数：`293`
- 当前待审核：`0`
- 当前正式资产：
  - 文件总数：`3961`
  - 模型：`156`
  - 图纸：`3805`
  - 总容量：`41,863,763,688 bytes`

### 105-启航华居项目

- 项目 ID：`503`
- 扫描任务：`414 / REAL_NAS_105`
- 扫描状态：`SUCCEEDED`
- 扫描文件数：`4052`
- 自动入库：`2927`
- 当前待审核：`0`
- 当前正式资产：
  - 文件总数：`2927`
  - 模型：`198`
  - 图纸：`2729`
  - 总容量：`35,338,680,331 bytes`

## 4. 路径追溯验收

通过平台 API 抽查：

- `100` 抽查 `25` 条文件资产。
- `105` 抽查 `25` 条文件资产。
- 合计抽查 `50` 条。

结果：

- NAS 路径存在：`50 / 50`
- 路径指向真实文件：`50 / 50`
- 平台登记大小与 NAS 文件大小一致：`50 / 50`

结论：路径追溯验收通过。

## 5. 检索与统计验收

平台 API 验证：

- `platform.admin` 可查询项目 `100`。
- `platform.admin` 可查询项目 `100` 文件资产列表。
- 项目统计接口可返回项目文件总数、模型数、图纸数和容量。

统计结果：

- `100`：文件 `3961`，模型 `156`，图纸 `3805`
- `105`：文件 `2927`，模型 `198`，图纸 `2729`

结论：项目资产检索与容量统计验收通过。

## 6. SQL View 验收

已验证以下视图：

- `ProjectAssetView`
- `FileAssetView`
- `ModelAssetView`
- `AuditEventView`

关键结果：

- `ProjectAssetView` 可查到 `100`、`105` 项目资产摘要。
- `FileAssetView` 可查到 `100`、`105` 的模型和图纸文件。
- `ModelAssetView` 可查到：
  - `100`：`156` 个模型，其中 `rvt 144`、`nwc 8`、`nwd 4`
  - `105`：`198` 个模型，均为 `rvt`
- `AuditEventView` 可查到项目创建、路径映射、扫描、自动入库、审核通过等动作。

结论：企业 agent 后续只读检索所需的核心视图已具备真实数据。

## 7. 权限验收

使用普通工程师账号 `delivery.engineer` 验证：

- 查询项目 `100`：返回 `0` 条。
- 查询项目 `100` 文件资产：返回 `0` 条。

使用管理员账号 `platform.admin` 验证：

- 查询项目 `100`：返回 `1` 条。
- 查询项目 `100` 文件资产列表：可返回数据。

结论：应用层项目权限隔离验收通过。

## 8. 审计与事件验收

审计日志：

- `100`
  - 自动入库审计：`3668`
  - 审核通过审计：`293`
  - 项目创建、路径映射创建、扫描创建、扫描运行均有审计。
- `105`
  - 自动入库审计：`2927`
  - 项目创建、路径映射创建、扫描创建、扫描运行均有审计。

事件流：

- `100`
  - 文件创建事件：`3961`
  - 审核通过事件：`293`
  - 项目、路径映射、扫描开始、扫描成功事件存在。
- `105`
  - 文件创建事件：`2927`
  - 项目、路径映射、扫描开始、扫描成功事件存在。

结论：审计与事件验收通过。

## 9. 暂停项复核

`101-C塔`：

- 扫描任务 `416 / REAL_NAS_101` 状态：`CANCELED`
- 真实 NAS 文件资产写入数：`0`

结论：`101` 没有污染当前真实试点数据，后续仍需先解决历史测试编码冲突。

## 10. 验收结论

真实 NAS 试点验收通过。

当前 `100` 和 `105` 已达到一期内部资产管理试点的核心要求：

- 项目可建档。
- NAS 元数据可入库。
- 文件路径可追溯。
- 模型/图纸可检索。
- 容量可统计。
- 权限可隔离。
- 审计与事件可追踪。
- 企业 agent 后续只读检索所需 SQL View 已具备真实数据。

## 11. 后续建议

1. 先不要全量扫描 27 个一级目录。
2. 下一步处理 `101-C塔` 的导入策略：清洁数据库、临时编码 `REAL-101`，或清理历史测试数据后正式导入。
3. 企业 agent 协议确定后，再做只读联调。
4. 再扩大试点时，建议每批 2-3 个项目，继续执行 `dry-run -> 写库 -> 路径抽查 -> 权限验收 -> SQL View 验收`。
