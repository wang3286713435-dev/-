# 真实 NAS 101-C塔清理与导入报告

时间：2026-05-09

## 1. 执行边界

- 用户确认执行：清理平台数据库中历史 `101` 测试污染，并复用项目编码 `101` 导入真实 NAS 项目。
- NAS 访问原则：只读真实 NAS，不修改、不移动、不删除 NAS 文件。
- 本轮仅修改平台数据库元数据和交接文档。

真实 NAS 路径：

`/Volumes/zyzn/卓羽智能项目/101-C塔`

## 2. 清理前快照

历史测试项目：

- 项目 ID：`99`
- 原项目编码：`101`
- 原状态：`ACTIVE`
- 原资产来源：`NAS_DISCOVERY`
- 活跃文件资产：`525`
- 活跃路径映射：`57`
- 活跃扫描任务：`1`
- 活跃扫描候选：`2074`
- 文件总大小：`8,920 bytes`
- 真实 NAS 文件记录：`0`

文件类型分布：

| 类型 | 数量 | 大小 |
| --- | ---: | ---: |
| ARCHIVE | 66 | 1,122 |
| DOCUMENT | 66 | 1,188 |
| DRAWING | 66 | 1,122 |
| MODEL | 129 | 1,990 |
| MODEL_VIEWER | 66 | 1,122 |
| PRESENTATION | 66 | 1,188 |
| SPREADSHEET | 66 | 1,188 |

判断：这些是历史批次测试数据，路径主要来自 `/tmp/bim-nas-*`，不是真实公司 NAS 数据，可以归档清理后复用编码 `101`。

## 3. 已执行清理

已归档旧项目：

- 旧项目 ID：`99`
- 旧编码：`101`
- 归档编码：`TEST-101-ARCHIVED-99`
- 项目状态：`INACTIVE`
- 资产状态：`INACTIVE`
- 删除标记：`deleted=1`

已软删除旧项目关联测试数据：

- `data_file_resources`：活跃记录清零。
- `data_asset_project_path_mappings`：活跃记录清零。
- `data_asset_scan_candidates`：活跃记录清零。
- `data_asset_scan_tasks`：活跃记录清零。
- `data_model_integrations`：无活跃记录。
- `data_managed_objects`：无活跃记录。
- `work_delivery_bindings`：无活跃记录。
- `data_asset_jobs`：无活跃记录。
- `data_asset_delete_requests`：无活跃记录。

审计与事件：

- 已写入 `core_audit_logs`：`project.archive-test-101.before`、`project.archive-test-101.after`。
- 已写入 `data_asset_events`：`archive-test-101`。
- traceId：`manual-real-101-cleanup`

## 4. 真实 101 项目入库

新建真实项目：

- 项目 ID：`505`
- 项目编码：`101`
- 项目名称：`C塔`
- 行业类型：`BUILDING_MEP`
- 阶段：`INTERNAL_PILOT`
- 资产来源：`NAS_REAL_PILOT`

新建真实路径映射：

- 路径映射 ID：`684`
- NAS 路径：`/Volumes/zyzn/卓羽智能项目/101-C塔`
- 匹配策略：`PREFIX`

扫描任务：

- 扫描任务 ID：`417`
- rootCode：`REAL_NAS_101`
- rootPath：`/Volumes/zyzn/卓羽智能项目/101-C塔`
- 递归扫描：`true`
- 扩展名白名单：`.rvt,.dwg,.ifc,.nwd,.nwc,.dxf,.pdf`
- 状态：`SUCCEEDED`
- 扫描开始：`2026-05-09 07:24:26 UTC`
- 扫描完成：`2026-05-09 07:31:50 UTC`
- 扫描文件数：`5666`
- 自动入库：`5457`
- 待审核：`0`
- 失败：`0`

说明：本轮真实 `101` 没有待审核候选，因此不需要人工审核处理。

## 5. 入库结果

正式文件资产：

- 文件总数：`5457`
- 总容量：`28,486,737,158 bytes`
- 模型文件：`114`
- 图纸文件：`5343`

按扩展名：

| 扩展名 | 数量 | 大小 |
| --- | ---: | ---: |
| pdf | 3927 | 7,949,790,038 |
| dwg | 1416 | 14,475,789,023 |
| ifc | 114 | 6,061,158,097 |

## 6. 验收结果

路径追溯：

- 抽样 `30` 条真实 `101` 文件资产。
- NAS 路径存在：`30/30`
- 指向文件：`30/30`
- 文件大小一致：`30/30`

SQL View：

- `ProjectAssetView` 可查到 `101 / C塔`。
- `FileAssetView` 可查到 `5457` 条 `101` 文件资产。
- `ModelAssetView` 可查到 `114` 条 `101` 模型资产。
- `AuditEventView` 可查到 `101` 的审计记录。

权限隔离：

- `platform.admin` 可查询 `101` 项目。
- `platform.admin` 可查询 `101` 文件资产。
- `delivery.engineer` 查询 `101` 项目返回 `0`。
- `delivery.engineer` 查询 `101` 文件资产返回 `0`。

审计与事件：

- `101` 审计记录：`5462`
- `101` 文件创建事件：`5457`
- `101` 路径映射创建事件：`1`
- `101` 项目创建事件：`1`
- `101` 扫描创建、开始、成功事件各 `1`

## 7. 结论

真实 `101-C塔` 已完成平台接管：

- 历史测试项目已软归档，不再占用编码 `101`。
- 真实项目 `101 / C塔` 已创建。
- 真实 NAS 路径已映射。
- 文件元数据已入库。
- 路径追溯、SQL View、权限隔离、审计事件均通过。

本轮没有修改、移动或删除任何真实 NAS 文件。

## 8. 后续建议

- `101` 全递归扫描耗时约 `7 分 24 秒`，虽然本轮成功，但真实 NAS 扫描体验应在后续优化为异步进度可见、可中断、可续扫。
- 下一步可以继续扩大真实试点，但建议每次先选择 `1-3` 个项目，避免一次性全量扫描造成 NAS 和本机开发环境压力。
