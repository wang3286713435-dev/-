# M1B 开发报告主 Agent 审计

时间：2026-05-20

## 审计结论

开发 agent 已完成 `M1B：项目工作台与数据管家可用性收口` 的开发报告与自测，修复方向符合 M1B 范围。

本轮可以进入测试 agent 验收。

## 范围审计

已确认：

- 未新增 Hermes 能力。
- 未继续 G4。
- 未进入 8B / 8C / 9A。
- 未开放真实 NAS 增删改查。
- 未读取文件正文。
- 未做真实 BIM 轻量化。
- 未修改 `docs/**`。
- 未修改后端或数据库迁移。

## 主要改动审计

### 资产总览

`AssetOverviewPage.vue` 已将页面口径从 G2 / 治理助手导向，收束为 M1B 项目资产入口：

- 第一屏解释“先选真实项目，再进入数字化交付工作区”。
- 工作顺序改为“看资产 -> 补底座 -> 做交付”。
- 默认真实 NAS 视图接入 `fetchAssetStatistics(...)`，用于展示项目数、文件数、模型文件、图纸文件和登记容量。
- 列表中的“治理路径”改为“下一步动作”，优先引导到主数据和文档交付。

### 项目工作台

`AssetProjectDetailPage.vue` 已弱化 Hermes：

- 主按钮改为“查看文档交付”。
- 增加“初始化主数据”入口。
- Hermes 保留为辅助入口，而不是主路径。
- 增加数据管家 / 工程主数据 / 工作中心三段说明。
- 模块卡片围绕资产驾驶舱、文件管理、初始化向导、部位树、交付物标准、文档交付、图纸交付、整改闭环等平台主线能力展开。

### 项目内导航

`ProjectWorkspaceNav.vue` 为三组导航增加说明：

- 数据管家：文件资产、预览和治理风险。
- 工程主数据：部位、节点类型和交付标准。
- 工作中心：文档图纸交付、审核整改和预检查。

## 非阻塞观察

- `AssetOverviewPage.vue` 中非默认筛选视图下，“图纸文件”统计仍可能无法从项目列表行精确汇总，因为列表行当前没有独立 `drawingCount` 字段。默认真实 NAS 视图使用统计接口，不影响本轮重点验收。
- 该观察可作为测试 agent 的 P2 判断项；若测试发现默认真实项目统计仍异常，则应判为 P1 并返修。

## 测试重点

测试 agent 应重点检查：

1. Fresh login 后资产总览第一屏是否能解释平台用途。
2. 默认真实项目统计是否不再明显为 0。
3. 503 / 105 和 506 / 93 都能进入项目工作台。
4. 项目工作台主路径是否从 Hermes 收回到文档交付 / 初始化主数据。
5. 项目内导航三组说明是否清楚且不横向撑爆。
6. 文件管理、工程主数据、文档交付、图纸交付入口不回归。
7. 不泄露真实 NAS 路径、SQL、token、secret。

## 当前裁决

可以进入 M1B 测试 agent 验收。

在测试 agent 返回报告前，不收口 M1B，不提交本轮开发改动。

